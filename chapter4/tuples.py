# Tuples example

# Tuples are immutable

myTuple = (78, 90, 20)

studentTuple = ("Parth", "veer", "Smit", "veer")

print(studentTuple[2])

# empty tuples

empty = ()
print(type(empty))

singleTuple = (1,)
print(type(singleTuple))
print(studentTuple.index("Parth"))
print(studentTuple.count("veer"))