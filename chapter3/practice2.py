# write a program that
    # Takes a sentence as input
    # Replaces all spaces " " with the undersores "_"
    # Prints the new string

Sentence = input("Please enter any sentenc:")

replace = Sentence.replace(" ", "_")

print(replace)